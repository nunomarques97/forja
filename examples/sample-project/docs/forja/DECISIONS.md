# Decisões — registo append-only

Uma linha por decisão tomada dentro de um run do Forja (quem, quando, o quê, porquê, reversível?). Nunca se apaga; uma decisão revertida ganha uma linha nova "substituída por".

- **D1** · 2026-09-16 20:23 · Bigorna · run R-20260916-8a30 — Página do sample segue um DESIGN.md mínimo próprio em docs/design/DESIGN.md (fundo escuro, tipografia de sistema, contraste AA, uma coluna, lista anti-slop), criado pelo Lapidador na task T3 antes da página; sem kickoff de 3 direções Porquê: Uma página estática de um projeto-exemplo não justifica 3 mocks; o DESIGN.md do repo Forja é do viewer, não deste sample; um ficheiro curto a $0 é o default mais barato e reversível; a direção visual vai à fila do Sponsor com o default aplicado Reversível: sim.
- **D2** · 2026-09-16 20:23 · Bigorna · run R-20260916-8a30 — slugify(text): String(text ?? "") → minúsculas → NFD e remoção de marcas combinantes (\p{M}) → qualquer sequência de caracteres fora [a-z0-9] vira um único hífen → sem hífens nas pontas; "", null e undefined devolvem "". Exportação: export nomeado em lib/slug.mjs, ficheiro irmão de lib/greet.mjs, sem re-export a partir de greet.mjs Porquê: "next to greet" lido como módulo irmão em lib/: greet.mjs continua sem imports (CLAUDE.md pede módulos em lib/ sem dependências) e cada módulo mantém uma responsabilidade; NFD é API nativa do runtime, sem instalação Reversível: sim.
- **D3** · 2026-09-16 20:23 · Bigorna · run R-20260916-8a30 — Texto da página: h1 "sample-project"; descrição "A deliberately tiny, dependency-free Node project that exists so Forja can validate its bootstrap on something real."; em inglês, como o README e o package.json Porquê: A description do package.json cita docs/ARCHITECTURE.md §13, referência interna ao repo Forja sem sentido numa página; a frase deriva da primeira linha do README; a regra de português do CLAUDE.md cobre saudações, não a página Reversível: sim.
- **D4** · 2026-09-16 20:23 · Bigorna · run R-20260916-8a30 — Ordem linear T1 slug → T2 limpeza data/legacy → T3 DESIGN.md → T4 página → T5 README, cada task com --after da anterior Porquê: O CLI aceita um único --after por task e o README (T5) precisa de T1 e T4 concluídas; a cadeia linear garante isso e o run é sequencial de qualquer forma; a lógica (T1) vai primeiro por ser a que mais desbloqueia e a mais fácil de testar Reversível: sim.
- **D5** · 2026-09-16 20:23 · Bigorna · run R-20260916-8a30 — A remoção de data/legacy (T2) fica dentro do run, sem ir à fila do Sponsor: a pasta é criada pela própria task com um placeholder e apagada de seguida com rm -rf data/legacy Porquê: Categoria 3 cobre apagar dados de utilizador ou ficheiros escritos pelo Sponsor; data/legacy não existe antes da task, nada está em git, e o único ficheiro removido é o placeholder que a própria task criou Reversível: sim.
- **D6** · 2026-09-16 22:26 · Product Manager · run R-20260916-ae6a — Âmbito do run: (a) servidor HTTP em Node puro que cria uma saudação com lib/greet.mjs e a grava num ficheiro JSON local; (b) página histórico servida por esse servidor, com a lista de saudações, um gráfico de saudações por dia e um formulário HTML simples para criar uma saudação sem terminal; (c) testes node --test para o servidor e para o HTML da página; (d) README com secção Como usar. Fora de âmbito: public/index.html (não muda), contas ou utilizadores, HTTPS, apagar ou editar saudações, paginação, base de dados, exportação, internet/túnel/deploy, arranque automático como serviço, rate limiting. O formulário na página é a única adição ao objetivo literal. Porquê: O objetivo pede uma app usável por 5 pessoas não técnicas: sem um formulário só se criava saudações por curl, o que esse público não faz; o resto do âmbito é exatamente o que o Sponsor escreveu e tudo o que ficou de fora é acrescento que ninguém pediu. Menos âmbito bem feito vale mais do que mais âmbito a compilar. Reversível: sim.
- **D7** · 2026-09-16 22:26 · Product Manager · run R-20260916-ae6a — Token: o servidor lê-o da variável de ambiente GREET_TOKEN e, se ela não existir, da primeira linha não vazia do ficheiro token.local.txt na raiz do projeto (adicionado ao .gitignore junto com data/). Sem token, ou com token com menos de 16 caracteres, o servidor NÃO arranca: sai com código diferente de zero e uma mensagem em português com o único passo para criar um token. Nunca há token por omissão nem token em código; nenhum agente gera, copia ou escreve um token (o README ensina o Sponsor a gerar o seu); o token nunca aparece em URL, log, notificação, commit ou relatório; a comparação é feita em tempo constante (crypto.timingSafeEqual) sobre hashes de igual comprimento. Os testes usam um token fictício definido no próprio processo. Porquê: Fail-closed é a única omissão segura: um servidor que arranca sem token aceita tudo o que estiver na rede. Env var serve quem arranca por terminal e o ficheiro local serve o Sponsor, que não vai mexer em variáveis de ambiente no Windows; ambos ficam fora do git. O mínimo de 16 caracteres impede tokens como 1234. As regras do Forja proíbem qualquer agente de criar ou manusear credenciais. Reversível: sim.
- **D8** · 2026-09-16 22:26 · Product Manager · run R-20260916-ae6a — Interface e porta: por omissão o servidor ouve em 127.0.0.1 (só o próprio computador), porta 8080; GREET_HOST e GREET_PORT permitem mudar, e GREET_HOST=0.0.0.0 é a forma explícita e documentada de o abrir à rede local. Ao arrancar, o servidor imprime em português onde está a ouvir e, quando está em 0.0.0.0, avisa que qualquer pessoa na mesma rede com o token pode entrar. A decisão de passar a ouvir na rede local é do Sponsor (Q3), com este default já aplicado. Porquê: Expor um serviço à rede é categoria 3 e não se liga por omissão. O loopback entrega tudo o que é preciso para construir e testar o run, e a passagem para a rede local fica a uma variável de distância, sem re-planeamento. 8080 é a porta convencional e livre para HTTP local. Reversível: sim.
- **D9** · 2026-09-16 22:26 · Product Manager · run R-20260916-ae6a — Sem perder dados, em concreto: (1) cada gravação escreve um ficheiro temporário na mesma pasta, faz fsync e só depois rename por cima do ficheiro final, para nunca existir um JSON meio escrito; (2) as gravações são postas em série dentro do processo (uma promessa encadeada), para dois pedidos ao mesmo tempo não se anularem; (3) ficheiro inexistente ou vazio no arranque é lista vazia e o ficheiro é criado na primeira saudação, sem erro; (4) ficheiro presente mas ilegível (JSON inválido ou formato inesperado) faz o servidor recusar arrancar e recusar gravar, com mensagem em português a dizer o caminho do ficheiro e o passo seguinte, e NUNCA sobrescreve, apaga, renomeia ou trunca esse ficheiro; (5) nunca há limpeza, rotação ou limite de registos; (6) pedidos são limitados a 4 KB de corpo e o nome a 80 caracteres, com recusa educada em português acima disso. Porquê: A prioridade número um do perfil é não perder dados. Escrita atómica mais serialização cobre o corte de energia e a concorrência; recusar em vez de reparar cobre o caso mau de verdade, porque qualquer reparação automática arrisca destruir o que lá está e isso é irreversível. Recusar arrancar é honesto para 5 pessoas numa sala e uma pessoa resolve em segundos com o README. Os limites de tamanho impedem que um pedido esquisito encha o ficheiro. Reversível: sim.
- **D10** · 2026-09-16 22:26 · Product Manager · run R-20260916-ae6a — Dados guardados: o ficheiro é data/greetings.json (caminho configurável por GREET_DATA) e cada registo tem exatamente id (crypto.randomUUID), nome tal como foi escrito, saudacao produzida por lib/greet.mjs e criado_em em ISO 8601 UTC. Não se guarda endereço IP, user-agent, cabeçalhos, nem qualquer vestígio do token. data/ e token.local.txt entram no .gitignore do sample-project, por isso nada disto vai para o git. A saudação é sempre a de lib/greet.mjs: português, a terminar em ponto de exclamação (CLAUDE.md). Porquê: Minimizar o que se guarda evita transformar isto em tratamento de dados pessoais novo, que seria categoria 3; um nome escrito por quem o escreveu é o próprio produto, um IP não é preciso para nada. O .gitignore próprio é necessário porque o sample pode ser copiado para fora do repo Forja, onde a regra data/ do repo-pai deixa de existir. Reutilizar greet() garante a regra de português do CLAUDE.md e evita uma segunda implementação. Reversível: sim.
- **D11** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — Como o browser passa o token: a API usa o cabeçalho, como o Sponsor pediu (criar saudação com X-Greet-Token). Como um browser não consegue pôr cabeçalhos ao navegar, existe uma página de entrada com um formulário HTML onde a pessoa cola o token uma vez; o servidor valida-o e devolve um cookie (HttpOnly, SameSite=Strict, Path=/) que vale para as visitas seguintes. Todos os pedidos, sem exceção, exigem token válido por cabeçalho ou por cookie: token errado ou ausente devolve 401 com uma frase em português e a ligação para a página de entrada, e nunca diz se o token existe ou quanto falhou. O token NUNCA vai em query string nem em qualquer URL. Como o formulário de criar saudação se autentica por cookie, os pedidos POST verificam também a origem (cabeçalho Origin/Referer contra o Host) e o Security Reviewer é gate obrigatório desta parte. Porquê: O objetivo diz cabeçalho e isso mantém-se para a API, mas uma equipa não técnica não consegue abrir uma página com cabeçalhos; sem isto a página histórico ou era inacessível ou ficava aberta a todos. O cookie é a forma mais simples que existe sem dependências, e HttpOnly mais SameSite=Strict, mais a verificação de origem, fecham o risco de CSRF que o cookie traz. Token em URL é o erro clássico: fica no histórico do browser e nos logs. Reversível: sim.
- **D12** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — Gráfico simples de saudações por dia: barras verticais em SVG inline, geradas no servidor a partir dos registos, uma barra por dia dos últimos 14 dias (dias sem saudações aparecem com barra a zero), com o número por cima ou ao lado de cada barra e a data por baixo; acompanhado sempre de uma tabela ou lista de texto com os mesmos números, que é o que um leitor de ecrã lê. Sem JavaScript no browser, sem biblioteca, sem eixos elaborados, sem tooltips, sem animação, sem cores novas além das de docs/design/DESIGN.md. A lista de saudações mostra as mais recentes primeiro, no máximo 100 por página de HTML, com o total indicado. Porquê: Sem dependências e sem JS, SVG gerado no servidor é a única forma honesta de desenhar barras, e é exatamente a escolha que o próprio Forja já fez para o viewer. Catorze dias cabem a 390 px sem scroll horizontal. A tabela de texto garante acessibilidade e serve de verificação nos testes, que passam a poder afirmar números sem ler geometria. Limitar a 100 linhas evita uma página de vários MB quando o ficheiro crescer, sem apagar nada. Reversível: sim.
- **D13** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — Língua e tom para público não técnico: tudo o que a equipa vê está em português simples, sem jargão: páginas, botões, mensagens de erro do browser e mensagens do terminal ao arrancar. Cada erro diz o que aconteceu e o único passo seguinte, numa frase; nunca stack traces, nunca códigos crus, nunca caminhos internos além do ficheiro que a pessoa precisa de abrir. O README passa a abrir com uma secção Como usar em português com passos copiáveis: criar o token, arrancar o servidor, abrir o endereço, parar o servidor, onde ficam as saudações, e o que fazer quando aparece cada uma das mensagens de erro possíveis. As secções técnicas existentes do README ficam em inglês e a frase inicial desatualizada (one module, one test) é corrigida. A página public/index.html mantém-se em inglês (D3). Porquê: O Sponsor definiu o público como não técnico e a equipa é portuguesa: uma mensagem em inglês ou um stack trace é uma paragem de trabalho para essas 5 pessoas. Manter as secções técnicas em inglês evita reescrever e traduzir documentação que já está revista, e mantém a coerência com D3 em vez de a reabrir. Reversível: sim.
- **D14** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — Design das páginas novas: reutilizam a direção já existente em docs/design/DESIGN.md (fundo escuro, tipografia de sistema, uma coluna, contraste AA, sem decoração, sem JS) e não há kickoff de três direções. O que falta nesse ficheiro para cobrir ecrãs novos (lista de saudações, barras do gráfico, formulário e campos, botão, mensagens de erro e estado vazio) é escrito pelo Product Designer numa secção nova do mesmo DESIGN.md, antes de existir HTML, e sem inventar cores fora da paleta. Definição de feito de qualquer página: screenshots reais a 1440 e 390 em docs/screenshots/, criticados contra o DESIGN.md. Isto continua ligado à Q2 em aberto: se o Sponsor recusar a direção, ambas as páginas são re-planeadas. Porquê: A direção visual já existe e está a aguardar confirmação do Sponsor na Q2; abrir uma segunda pergunta sobre o mesmo gosto visual só atrasava. Estender o DESIGN.md é mais barato e mais coerente do que um kickoff novo para um projeto-exemplo, e mantém a regra de que nenhuma UI nasce sem direção escrita. Reversível: sim.
- **D15** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — Tecnologia: este run usa apenas built-ins do Node 24 (node:http, node:fs e fs/promises, node:path, node:crypto, node:test, node:assert) mais HTML, CSS e SVG escritos à mão. Zero dependências, zero ficheiros novos em package.json além de scripts, nenhuma framework, nenhum bundler, nenhuma base de dados, nenhum CDN. Não é preciso acordar o Technology Scout nem criar docs/forja/TECHNOLOGY.md neste run: não há tecnologia nova a escolher. Qualquer necessidade que um Dev julgue exigir algo fora desta lista para BLOCKED com a justificação, e é o Lead que acorda o Scout. Porquê: O CLAUDE.md do projeto obriga a módulos sem dependências e a testes só com node --test, e tudo o que o objetivo pede cabe nos built-ins. Registar isto por escrito evita o bloqueio previsto na regra nenhum Dev introduz tecnologia sem decisão do Scout, que de outra forma pararia o run por uma escolha que já está feita. Reversível: sim.
- **D16** · 2026-09-16 22:27 · Product Manager · run R-20260916-ae6a — O sample-project continua a ser o repositório de exemplo que valida o forja bootstrap, e a app é aditiva: no fim do run, npm test tem de correr verde sem servidor ligado nem token no ambiente, lib/greet.mjs e lib/slug.mjs mantêm-se sem imports e sem alterações de comportamento, o bloco forja do CLAUDE.md não é tocado à mão, e nenhum estado de execução (data/, token.local.txt) entra em git. A abertura do README passa a dizer as duas coisas que este projeto é: fixture do bootstrap e pequena app de saudações da equipa. Porquê: O objetivo do Sponsor muda o que o projeto faz mas não pode apagar a razão por que ele existe dentro do repo Forja; se o bootstrap deixar de poder ser validado aqui, o run causou um estrago maior do que o valor que entregou. Testes que dependessem de um token no ambiente tornariam o npm test não reprodutível para quem faz bootstrap. Reversível: sim.
- **D17** · 2026-09-16 22:28 · Product Manager · run R-20260916-ae6a — Fasquia de prova deste run: os testes do servidor arrancam-no em 127.0.0.1 na porta 0 (porta efémera) com um token fictício e um ficheiro de dados temporário, e cobrem no mínimo: pedido sem token e com token errado recusado; pedido com token no cabeçalho aceite; saudação gravada e relida do ficheiro; arranque com ficheiro inexistente; ficheiro inválido não é sobrescrito; duas gravações ao mesmo tempo não perdem nenhuma; corpo acima do limite recusado. Os testes da página são asserções sobre o HTML gerado pelo servidor (contagens por dia, ordem da lista, ausência de script e de http:// externo, estado vazio), sem browser. A prova visual são os screenshots reais a 1440 e 390 do helper do Forja. Um relatório sem saída de npm test e sem os dois caminhos de screenshot não fecha uma task. Porquê: Sem perder dados é uma afirmação que só vale testada: cada regra do D9 tem de ter um teste que falharia sem ela. Porta 0 evita colisões e testes instáveis; o HTML é testável sem browser porque não há JS no cliente, o que mantém os testes em node --test como o CLAUDE.md exige, e os screenshots continuam a ser a única prova de que a página está bem. Reversível: sim.
- **D18** · 2026-09-16 23:49 · Product Designer · run R-20260916-ae6a — direção A (lista primeiro) para os ecrãs histórico, entrar e erro Porquê: Tres direcoes construidas como mocks reais com dados de lib/greet.mjs e lib/stats.mjs e fotografadas a 1440 e 390 (docs/design/mocks/). A e a unica em que, a 390 e sem rolar, se ve o que a app e (saudacoes), onde se escreve (campo + botao) e a prova de que funciona (lista); a saudacao nova nasce logo por baixo do botao, que e o ciclo da fasquia de qualidade do perfil. B poe o grafico e um numero grande primeiro e enterra a unica acao; C acerta na acao mas veste a lista de monoespacado e adia o historico. Regras fixadas em docs/design/DESIGN.md, seccao Ecras servidos pela aplicacao; razoes completas em docs/design/mocks/README.md Reversível: sim.
- **D19** · 2026-09-17 03:00 · Backend Dev · run R-20260916-ae6a — O cookie greet é um cookie de sessão: sessionCookie() em lib/auth.mjs devolve `greet=<valor url-encoded>; HttpOnly; SameSite=Strict; Path=/`, sem Max-Age e sem Expires, por isso o browser deixa-o cair quando fecha. Substitui a parte «Max-Age=2592000» do critério 5 de T2 (task já done, texto não editável em docs/forja/TASKS.json); a fonte de verdade passa a ser docs/design/DESIGN.md §4 ponto 4. Porquê: finding 1 do QA (docs/forja/reports/QA-close-1.md) — a página Entrar promete «Fica guardada neste browser até o fechar e nunca sai desta rede.» e o cookie durava 30 dias; numa máquina partilhada de escritório quem fecha o browser a pensar que saiu tem de ficar mesmo fora, e o texto e o código passam a dizer o mesmo. Corrigiu-se o cookie e não a frase porque a frase é a promessa mais segura das duas e é o contrato visual aprovado (D18). Reversível: sim.
- **D20** · 2026-09-17 03:46 · Product Manager · run R-20260916-ae6a — Fecho da Q3: com o «Sim» do Sponsor, ouvir na rede local passa a estar autorizado, mas continua a ser opt-in por arranque, nao o default. O servidor mantem 127.0.0.1 como omissao e abre-se a rede com GREET_HOST=0.0.0.0, que desde T10 imprime os enderecos a dar aos colegas e o aviso, tudo explicado no README. Mudar o default embutido para 0.0.0.0 fica fora de ambito deste run. Porquê: «Sim» autoriza, nao obriga: o mesmo portatil vai para casa, para um cafe ou para uma rede de convidados, e um default aberto expunha a app sem ninguem decidir nada nesse momento. Opt-in por arranque da a mesma capacidade com uma variavel e um passo copiavel do README, e e reversivel sem tocar em codigo. Fica assim registado o que a resposta do Sponsor mudou (o caminho existe e esta documentado) e o que nao mudou (a omissao segura). Reversível: sim.
- **D21** · 2026-09-17 03:46 · Product Manager · run R-20260916-ae6a — Os 6 findings menores do QA (docs/forja/reports/QA-close-2.md) nao bloqueiam o fecho do run R-20260916-ae6a: ficam propostos como trabalho futuro, por esta ordem — (1) sem sessao aterrar em /entrar em vez de 401; (2) tirar o fragmento em ingles da mensagem de arranque com ficheiro de dados ilegivel; (3) README deixar de prometer que qualquer endereco de rede serve; (4) pagina 500 no formato do DESIGN.md §5; (5) linha de 403 no DESIGN.md com um h1 que diga o que aconteceu; (6) <title> do grafico dizer «nestes 14 dias». Nenhum toca em dados gravados, seguranca ou no papel de fixture do bootstrap. Porquê: O objetivo do Sponsor cumpre-se ponta a ponta com 134/134 testes verdes, os dois findings graves fechados por T9 e T10 e verificados no produto a correr; nenhum dos 6 restantes impede alguem de entrar, gravar e ver saudacoes, e nenhum arrisca perder dados. Segurar o fecho do run por polimento seria trocar entrega por perfeicao; a ordem proposta poe primeiro o que a fasquia de qualidade do perfil mede (os primeiros 10 segundos e portugues sem excecoes) e deixa o formato dos ecras de erro para o fim. Reversivel: e so uma lista de trabalho proposto, o Sponsor reordena ou descarta. Reversível: sim.
