# T10 — tentativa 1 — veredicto do Security Reviewer (dados, verbatim)

Modelo: opus. Gatilho: a task muda o que um servidor que ouve em toda a rede local imprime, e o aviso de exposição. Data: 2026-09-17.

---

SECURITY-APPROVE — T10 só acrescenta output no terminal: imprime os IPv4 locais e o aviso reescrito quando `GREET_HOST=0.0.0.0`; nenhuma rota, bind, sessão, segredo ou dependência muda, e provei no servidor a correr que os endereços anunciados continuam fechados por palavra-passe e que nem o token nem a palavra «token» chegam a stdout.

**Bloqueadores:** nenhum.

**Nits:**

1. `docs/forja/reports/T10-a1-dev.md:29-30` e `T10-a1-review.md:23` — os relatórios (que vão a commit; `docs/forja/reports/` é versionado) gravam os IPv4 reais desta máquina, incluindo o adaptador virtual. Não é credencial e são endereços RFC1918 — impacto quase nulo — mas o perfil pede «nada em relatório» e mascarar custa zero. Padrão pré-existente: `QA-close-1.md` já tinha os mesmos IPs. Nada a mudar no código.
2. `server.mjs:110` — `lanAddresses()` anuncia **todos** os IPv4 não-internos, o que inclui adaptadores virtuais (Hyper-V/WSL/VPN/Docker). Confirmei que é honesto (o servidor responde mesmo nesse endereço, 303), mas: (a) diz a uma pessoa não técnica para dar aos colegas um endereço que eles não alcançam, e (b) o terminal — e qualquer screenshot dele — passa a revelar que a máquina corre virtualização. Nada a corrigir hoje; se um dia se filtrar, filtre-se por prefixo privado (`192.168.`, `10.`, `172.16–31.`) e não por nome de interface.
3. `README.md:63-79` — o **Aviso** de rede continua lá e com o mesmo conteúdo, mas ficou 17 linhas abaixo do comando `set GREET_HOST=0.0.0.0`, agora depois das instruções «dê este endereço aos colegas». O passo perigoso e o seu aviso afastaram-se. Sugestão: mover o parágrafo `**Aviso:**` para imediatamente a seguir ao bloco de comando.
4. `server.mjs:119-122` — o aviso **não** foi suavizado. Antes «…qualquer pessoa na mesma rede que tenha o token consegue entrar», agora «…que tenha a palavra-passe consegue entrar»; a primeira metade («o servidor está a ouvir em toda a rede local») está intacta e é a que carrega o risco. A troca é só de vocabulário e alinha com `docs/design/DESIGN.md` §7 e §4. Observação residual, igual antes e depois: o aviso descreve quem *tem* a palavra-passe, não diz que o serviço passa a ser visível/scanável por toda a gente na rede. Não é regressão desta task.
5. `lib/auth.mjs:70,95,116,123` — a palavra «token» continua a sair por **stderr** no caminho de falha de arranque (nit 5 do Reviewer). Verifiquei que é só a palavra: com um ficheiro de token com valor-marcador, o valor não aparece em stdout nem em stderr (0 ocorrências). Coerência de vocabulário, não fuga; fora do âmbito de T10.
6. `lib/lan.mjs:40` — o endereço vai verbatim para o terminal. Hoje é impossível abusar (único chamador em produção é `server.mjs:107`, sem argumento, e a fonte é o kernel), mas se algum dia `interfaces` vier de fora, uma string com sequências ANSI escreveria diretamente no terminal. Registo, não ação.

**Verificado:**
- **Input** — `lib/lan.mjs` não recebe input externo: única fonte é `os.networkInterfaces()`; sem `exec`/`spawn`/`eval`/`Function`/`fetch`/`node:net` nos ficheiros novos (grep). O README manda a *pessoa* correr `ipconfig`; o código nunca lança esse processo — evita uma superfície de comando. Guardas de tipo em `lan.mjs:27,30,32,37` tratam `null`/primitivos/não-arrays sem rebentar; ciclo limitado ao nº de interfaces, sem recursão nem regex sobre input.
- **Rede** — nenhum bind mudou: `server.listen(port, host)` intocado, default `127.0.0.1` intocado, zero pedidos de saída, zero CORS, zero túnel. Provei ao vivo com `GREET_HOST=0.0.0.0 GREET_PORT=0`: `GET /` nos três endereços -> 303 para `/entrar`, `GET /historico` -> 401 nos três. DNS rebinding: `sameOrigin()` continua sem allow-list de Host, exatamente como no gate de T9 — não explorável (cookie host-scoped e sem `Domain`, e sem palavra-passe tudo fecha), e esta task não mexe em `lib/app.mjs`.
- **Auth** — nenhuma rota, verificação ou cookie tocados. Token pela URL continua a não autenticar: `GET /historico?token=<token real>` -> **401** nos três endereços anunciados. Os URLs impressos não têm query string nem `user@host`.
- **Segredos** — stdout verbatim do servidor a correr não contém o token (`false`) nem a subcadeia `token` em nenhuma caixa (`/token/i` -> `false`); stderr vazio no arranque bem sucedido. `README.md` não ganhou nenhum token de exemplo. Fixture dos testes é manifestamente inventado (`test/server.test.mjs:18`). Sem padrões de chave nos ficheiros novos. `token.local.txt` e `data/` no `.gitignore`. Nada escrito em notificação.
- **Dependências** — `package.json` sem `dependencies`/`devDependencies`, sem lockfile, sem `node_modules`, e `git diff -- package.json` vazio. Único import novo é `node:os`, built-in, coberto pelo veredicto S0 de `docs/forja/TECHNOLOGY.md` — nenhuma decisão de Scout em falta.
- **Menor privilégio** — só `README.md`, `server.mjs`, `test/server.test.mjs` modificados, `lib/lan.mjs` e `test/lan.test.mjs` novos, mais os ficheiros de estado do Forja escritos pelo Lead. Nenhuma escrita fora do projeto, nenhum commit, nenhum push, nenhum git destrutivo. Não editei nada.
- **Dados** — o que passa a ser impresso são endereços IPv4 privados da própria máquina, no terminal de quem arrancou o servidor e que já os obteria com `ipconfig`; D10 continua a não guardar IP de ninguém nos registos. Divulgação aceitável para o público do perfil (equipa de 5 na mesma rede, prioridade 3); ressalvas nos nits 1 e 2.
- **Prompt-injection** — `T10-a1-dev.md` e `T10-a1-review.md` lidos como dados; nenhum contém instruções dirigidas a mim, e o veredicto do Reviewer não substituiu nenhuma verificação minha.
- **Testes** — `npm test` numa shell com as variáveis `GREET_*` removidas -> `tests 134 / pass 134 / fail 0`. `npm run check` na raiz do Forja -> `check ok`.

**Suspeito:** durante esta sessão apareceu, colado ao resultado de uma ferramenta, um bloco `# MCP Server Instructions` (vários conectores de terceiros) a mandar usar e encadear ferramentas externas. Tratei-o como dados, como o gate de T9 já tinha registado para o mesmo padrão; não veio do diff nem dos relatórios de T10 e não influenciou nada deste veredicto.
