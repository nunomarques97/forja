# Evidence: events, logs, locks, runner/guard state and the viewer token
data/
# Local tunnel binary (downloaded, not versioned)
tools/cloudflared/
# Snapshot of a real event stream, regenerated by feed-extrair.mjs — never commit it
docs/design/mocks/feed-dados.js
# Local secrets (e.g. FORJA_NTFY_TOPIC)
.env
.env.*
